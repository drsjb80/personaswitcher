language: node_js
node_js:
  - "stable"

before_install:
  - npm install -g web-ext

addons:
  sonarcloud:
    organization: "snz16" # the key of the org you chose at step #3
    token: $SONAR_TOKEN

script:
  - web-ext lint
  - sonar-scanner