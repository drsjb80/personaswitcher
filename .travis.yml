language: node_js
node_js:
  - "12.14.1"

addons:
  sonarcloud:
    organization: "snz16" # the key of the org you chose at step #3
    token:
      secure: ${{ secrets.SONAR_TOKEN }}

script:
  - npm run lint
  - sonar-scanner