{
  "name": "personaswitcher",
  "version": "1.0.0",
  "dependencies": {
    "jest": "^25.1.0"
  },
  "scripts": {
    "test": "jest",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  }
}
